import random
A=[]
for _ in range(0,10000000):
    X=random.randint(0, 1000)
    Y=random.randint(0, 1000)
    if X>Y: 
        A.append(X)
    else:
        A.append(Y)
sum(A) / len(A)
print(sum(A) / len(A))
